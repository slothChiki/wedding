export const enum Navigation {
    KAKAO = 0,
    TMAP = 1,
    NAVER = 2,
}

export const enum MainManu {
    MOVIE_INFO = 0,
    THEATER_INFO = 1,
}

export const enum NavMenu {
    MAIN = 0,
    CAST = 1,
    ALBUM = 3,
    BOOKING = 4,
    REVIEW = 5,
}

export const enum PosterTyep {
    TOP10 = 0,
    GENERAL = 1,
}

export const enum moveMedia {
    PC = 1024,
    TAB = 768,
    MOBILE = 460,
}

export const enum imageMedia {
    PC = 230,
    TAB = 180,
    MOBILE = 160,
}
export const enum top10Media {
    PC = 320,
    TAB = 250,
    MOBILE = 220,
}

export const enum Sns {
    TWITTER = 0,
    FACEBOOK=1,
    INSTARGRAM=2,
}
