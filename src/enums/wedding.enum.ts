export enum Navigation {
    KAKAO = 0,
    TMAP = 1,
    NAVER = 2,
}

export enum MainManu {
    MOVIE_INFO = 0,
    THEATER_INFO = 1,
}

export enum NavMenu {
    MAIN = 0,
    CAST = 1,
    ALBUM = 3,
    BOOKING = 4,
    REVIEW = 5,
}

export enum PosterTyep {
    TOP10 = 0,
    GENERAL = 1,
}

export enum moveMedia {
    PC = 1024,
    TAB = 768,
    MOBILE = 460,
}

export enum imageMedia {
    PC = 250,
    TAB = 180,
    MOBILE = 160,
}
export enum top10Media {
    PC = 320,
    TAB = 250,
    MOBILE = 220,
}

export enum Sns {
    TWITTER = 0,
    FACEBOOK = 1,
    INSTARGRAM = 2,
}

export enum ModalType{
    LOCATION=0,
    POSTER=1,
    BOARD=2,
    
}

export enum DetailType {
    IMG = 0,
    CONTENTS = 1,
    ACTOR = 2,
}

export enum DetailMenu {
    ACTOR = 0,
}

export enum TimerType {
    DDAY = 0,
    FLOW = 1,
}
