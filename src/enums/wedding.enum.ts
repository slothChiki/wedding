export enum Navigation {
    KAKAO = 0,
    TMAP = 1,
    NAVER = 2,
}

export enum MainManu {
    MOVIE_INFO = 0,
    THEATER_INFO = 1,
}

export enum moveMedia {
    PC = 1024,
    TAB = 768,
    MOBILE = 460,
}

export enum imageMedia {
    PC = 250,
    TAB = 180,
    MOBILE = 160,
}
export enum top10Media {
    PC = 320,
    TAB = 250,
    MOBILE = 220,
}

export enum Sns {
    TWITTER = 0,
    FACEBOOK = 1,
    INSTARGRAM = 2,
}

export enum DetailType {
    IMG = 0,
    CONTENTS = 1,
    ACTOR = 2,
    LOCATION = 3,
    BOARD = 4,
}

export enum TimerType {
    DDAY = 0,
    FLOW = 1,
}

export enum ActorType {
    NY = 0,
    SM = 1,
}

export enum SliderType {
    IMG = 0,
    CONTENTS = 1,
    ACTOR = 2,
}
